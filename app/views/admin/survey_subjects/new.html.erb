<% content_for :title, 'New Survey' %>
<% content_for :breadcrumb, 'New' %>
<% breadcrumb_add 'Surveys', admin_survey_subjects_path %>

<div class="twelvecol">
    <div class="page-header">
        <h2 class="widget widget-header">New Survey</h2>
    </div>
    <%= render partial: 'form', locals: { url: admin_survey_subjects_path } %>
</div>

